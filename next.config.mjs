/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'source.unsplash.com',
      'upload.wikimedia.org',
      'i.imgur.com'
    ],
  },
};

export default nextConfig;
